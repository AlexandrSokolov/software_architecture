## Lombok Example

This example requires Lombok support enabled in the IDE.

It is intentionally isolated to avoid contaminating the core
Java examples with Lombok-specific dependencies.

Compile/run requirements:
- IDE with Lombok plugin enabled